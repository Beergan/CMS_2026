@page
@model CMS_2026.Pages.Admin.Category.IndexModel
@{
    ViewData["Title"] = "Danh sách chuyên mục";
    Layout = "_Layout";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <h4 class="page-title">Danh sách chuyên mục</h4>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card-box table-responsive">
                <ul class="nav nav-tabs">
                    <li class="nav-item">
                        <a href="#list" data-toggle="tab" aria-expanded="true" class="nav-link active">
                            <i class="fas fa-list-ol"></i> Danh sách chuyên mục
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/admin/category/create?type=@Model.NodeType" aria-expanded="false" class="nav-link">
                            <i class="fas fa-plus-circle"></i> Tạo chuyên mục mới
                        </a>
                    </li>
                </ul>
                <div class="tab-content">
                    <div class="tab-pane show active" id="list">
                        <table id="datatable" class="table table-striped table-bordered dt-responsive">
                            <thead>
                                <tr>
                                    <th>Tiêu đề</th>
                                    <th>Đường dẫn</th>
                                    <th>Loại</th>
                                    <th style="width:1%;min-width:80px;">Thao tác</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var category in Model.Categories)
                                {
                                    <tr>
                                        <td>@category.Title</td>
                                        <td>@category.CategoryPath</td>
                                        <td>@category.NodeType</td>
                                        <td align="center">
                                            <div class="btn-group btn-group-sm">
                                                <a title="Hiệu chỉnh" href="/admin/category/edit?id=@category.Id" type="button" class="btn btn-warning">
                                                    <span class="mdi mdi-pencil"></span>
                                                </a>
                                                <button title="Xóa" type="button" class="btn btn-danger" onclick="deleteCategory(@category.Id, '@category.Title')">
                                                    <span class="mdi mdi-delete"></span>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts {
    <script>
        function deleteCategory(id, title) {
            if (!confirm('Bạn có chắc chắn muốn xóa chuyên mục "' + title + '"?')) {
                return;
            }

            $.ajax({
                url: '/admin/category?delete',
                type: 'POST',
                data: {
                    __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').val(),
                    Id: id
                },
                success: function(response) {
                    if (response.success) {
                        alert(response.message);
                        location.reload();
                    } else {
                        alert(response.message);
                    }
                },
                error: function() {
                    alert('Có lỗi xảy ra!');
                }
            });
        }
    </script>
}

